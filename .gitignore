# local configuration: may hold an API key. .env.example is the committed template.
.env
.env.*
!.env.example

# runtime data: chroma store, embedding model cache, bm25 artifacts, audit log
data/

# generated code graph: ~4MB, rebuilt in seconds with `graphify update .`
# Un-ignore it if the team wants the graph under review; graphify ships a merge
# driver for exactly that.
graphify-out/

# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# editors / os
.DS_Store
.idea/
.vscode/
